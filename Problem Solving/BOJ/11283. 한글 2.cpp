#include <bits/stdc++.h>

using namespace std;

int main(void)
{
	
	wchar_t s;
	wcin >> s;
	string str = s;
	WCHAR *wpChar;
	char *ch = LPSTR(LPCTSTR(m_strRead));
	int len;
	len = MultiByteToWideChar(CP_ACP, 0, ch, m_strRead.GetLength(), wpChar, sizeof(wpChar));

}
